services:
  - type: web
    name: bramhastra-api
    runtime: python
    repo: https://github.com/CalculusGuy/BRAMHASTRA
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
